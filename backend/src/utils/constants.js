export const ROLES = Object.freeze({
  ADMIN: 'admin',
  SALES_PERSON: 'sales_person',
  USER: 'user',
});

export const ORDER_STATUS = Object.freeze({
  CREATED: 'created',
  PAID: 'paid',
  FAILED: 'failed',
});
